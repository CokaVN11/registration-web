Should change the .env file to your own values. (Modify the `.env.example` file and rename it to `.env`)

To run this app: 
```bash
pnpm install

# Start the server
pnpm start:server

# Start the client
pnpm dev:client
```